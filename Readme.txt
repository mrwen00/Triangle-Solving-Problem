You can run main.exe to use, or try compile it again by compiler g++
Open your terminal and then type: g++ *.cpp -o main -std=c++11

The input file is test.txt, the program will load this file to proceed
